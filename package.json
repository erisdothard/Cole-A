{
  "name": "cvz-system",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "CVZ — interactive portfolio. Not a portfolio; a machine that contains a creative world.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
